<Page x:Class="CarSystemGUI.Cars"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarSystemGUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Cars">
    <ScrollViewer VerticalScrollBarVisibility="Visible" >

        <Grid Name="gr" Background="White"  ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="400"/>
            </Grid.RowDefinitions>
            <Label Content="All brands" HorizontalAlignment="Center" FontSize="30"/>
            <Label Name="lbl" HorizontalAlignment="Right" FontSize="50"/>
            <Button Content="Add new brand" Height="30" Width="100" Click="Button_Click"
                    Background="Transparent"  VerticalAlignment="Top" HorizontalAlignment="Right"/>
        </Grid>
    </ScrollViewer>
</Page>
